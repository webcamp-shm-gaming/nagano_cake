<div>
  <h1>商品一覧</h1>
  <% @items.each do |item| %>
  <div>
　　<%= link_to item_path(item.id) do %>
　　 <%= image_tag item.image if item.image.attached? %>
　　<% end %>
　　<p><%= item.name %></p>
　　￥<%= item.price.to_s(:delimited) %>
　<% end %>
  </div>
</div>
