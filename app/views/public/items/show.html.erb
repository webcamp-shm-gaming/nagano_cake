<div>
   <%= image_tag @item.image, size: "200x200" %>
  <h2><%= @item.name %></h2>
  <p><%= @item.introduction %></p>
　<span>￥<%= @item.with_tax_price.to_s(:delimited) %>（税込）</span>
　<p><%= form_with model: @item do |f| %>
　     <%= f.select :amount, *[1..5] %>
　   <% end %>
　   <%= link_to "カートに入れる", cart_items_path, class: "btn btn-sucess" %>
　</p>
</div>
