<<<<<<< HEAD
<div class="container">
  <div class="row">
    <div class="col-4 mr-3">
     <%= image_tag @item.image, size: "400x400" %>
    </div>
    <div class="col-sm-12 col-md-8 col-lg-5 px-5 px-sm-0 mx-auto">
      <div class="form-group">
        <h2 class="mt-3"><%= @item.name %></h2>
        <p class="mt-3"><%= @item.introduction %></p>
        <span class="col-3">￥<%= @item.with_tax_price.to_s(:delimited) %>（税込）</span>
           <%= form_with model: @cart_item, url: cart_items_path do |f| %>
            <p class="col-4"><%= f.select :amount, *[1..5] %></p>
            <!--item_idというキーに対して@item.idという値をブラウザに表示せずに送信-->
            <p class="col-4"><%= f.hidden_field :item_id, :value => @item.id %>
            <%= f.submit "カートに入れる", class: "btn btn-success" %>
           <% end %>
           </p>
      </div>
    </div>
  </div>
=======
<div>
   <%= image_tag @item.image, size: "200x200" %>
  <h2><%= @item.name %></h2>
  <p><%= @item.introduction %></p>
  <span>￥<%= @item.with_tax_price.to_s(:delimited) %>（税込）</span>
     <%= form_with model: @cart_item, url: cart_items_path do |f| %>
      <%= f.select :amount, *[1..5] %>
      <%= f.submit "カートに入れる", class: "btn btn-sucess" %>
     <% end %>
>>>>>>> develop
</div>
